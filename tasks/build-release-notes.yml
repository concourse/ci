---
platform: linux

image_resource:
  type: registry-image
  source: {repository: concourse/unit}

inputs:
- name: release-me
- name: ci
- name: concourse

outputs:
- name: built-notes

params:
  GITHUB_TOKEN:
  GITHUB_BRANCH:

run:
  path: ci/tasks/scripts/build-release-notes
