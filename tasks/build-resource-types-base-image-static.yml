---
platform: linux

params:
  REPOSITORY: concourse/resource-types-base-image-static
  DOCKERFILE: ci/dockerfiles/resource-types-base-image-static/Dockerfile
  CONTEXT: .
  IMAGE_ARG_base_image: cgr.dev/chainguard/busybox
  IMAGE_PLATFORM: linux/amd64
  OUTPUT_OCI: true

inputs:
- name: ci
- name: resources-types-base-image

outputs:
- name: image

caches:
- path: cache

run:
  path: build
